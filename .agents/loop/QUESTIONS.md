# ARWAKY LOOP QUESTIONS

FRD ambiguities / open questions surfaced by the loop:

* **RESOLVED (Cycle 25)** : Entire CLI capability/agent layer deleted; `CliContainer` was never instantiated and the CLI surface routes directly through the DI container.
* **RESOLVED (Cycle 28)** : Cleaned up 20 orphaned job and MCP capability/protocol/taxonomy files verified dead via full-repo grep; core FR coverage remains intact.
* **RESOLVED (Cycle 55)** : AES501/504 orphan flags — SKIPPED. All violations AES501–AES505 are suppressed indefinitely. Do not work on these violations. They produce too many false positives and are not worth the noise.
* **OPEN (Cycle 46)** : AES201 flagged `surface_cli_command.py` importing from `agent_` prefix file — confirmed false positive (file lives in shared/src/common/, not agent layer). Linter's prefix-based detection cannot distinguish `agent_di_container.py` (shared DI utility) from actual agent orchestrators. Should the linter be configured to use path-based layer detection instead of filename-prefix?
* **RESOLVED (Cycle 55)** : AES304 bypass comments (439) — DECISION: resolve per-file with targeted type widening (surgical approach). No bulk suppression lists. Address highest-risk files first when working on a capability file.
* **RESOLVED (Cycle 49)** : AES201 broken import chain — FIXED by deleting dead/orphan files (`surface_cli_command.py` with CliCommandHandler, `root_cli_entry.py`) that imported from non-existent `modules.shared.src.common.agent_di_container`. AES201 violations reduced to 0.
* **RESOLVED (Cycle 50)** : AES202 barrel false positives — ACCEPTED as intentional false positives (barrel pattern + GatewayOrchestrator design). Documented in ASSUMPTIONS.md and AUDIT.md. No code changes required.
* **RESOLVED (Cycle 55)** : AES502 contract orphan remediation — DECISION: (a) delete all 57 orphan contracts and update barrel exports in `shared/src/__init__.py`. Clean break approach. No implementation of dead contracts.
* **RESOLVED (Cycle 40)** : Stale `blender-arwaky` entry point fixed in `pyproject.toml` to reference `modules.cli.src.surface_cli_main:main`.
* **RESOLVED (Cycle 52)** : Broken barrel export — FIXED by correcting import paths in `modules/shared/src/job/__init__.py` and `modules/shared/src/__init__.py` from non-existent `taxonomy_job_state_constant.py` to `taxonomy_job_constant.py`. Recovered 4 test collection errors. Total tests: 453.
* **RESOLVED (Cycle 53)** : AES302/AES403 false positives in capabilities_job_monitor.py — confirmed false positive (file has complete docstrings, implements IJobMonitor protocol). Linter's ABC detection may not track inheritance chains correctly. No code changes required.
* **RESOLVED (Cycle 54)** : FR-GWY-002 reconnect attempt-counter — `MaintenanceExecutor._reconnect_attempts` accumulated across reconnect sessions (stale count / premature exhaustion on later drops). Fixed to reset per session; 2 regression tests added. Full suite 453 pass.
* **OPEN (Cycle 60)** : MCP tool routing targets (FR-MCP-002 parity with CLI). `ToolRegistryHandler.register_tools()` ImportError was fixed (tools now register), but the four tool handlers delegate to `core_agent_orchestrator` (DispatcherOrchestrator), which only implements `execute_action` (sync). It does NOT expose `list_commands`, `read_skill_context`, or `health_check` — so those three tools would raise `AttributeError` at call time, and `execute_command`'s `await orchestrator.execute_action(...)` raises `TypeError` (await on a sync method). Two design questions: (a) Should these methods be ADDED to the dispatcher aggregate/orchestrator, or should the surface route `list_commands`→`discover_actions`/catalog, `health_check`→diagnostics snapshot, `read_skill_context`→the discovery protocol? (b) Should `execute_action` be made async (or the surface stop awaiting it)? The CLI surface itself routes via `BlenderSocketClient`, not the dispatcher aggregate, so "1:1 parity with CLI" needs a concrete target map. Needs user/architecture decision before fixing — risky to change the orchestrator's public contract autonomously alongside concurrent sibling edits.
* **OPEN (Cycle 62)** : Repo-wide import break from concurrent sibling scene refactor. Untracked `modules/shared/src/scene/contract_scene_protocol.py` imports non-existent `taxonomy_scene_vo` (VOs actually live in `taxonomy_scene_command_vo.py`), and `contract_scene_aggregate.py` (modified) imports stale protocol names `SceneInspectionProtocol`/`SceneCleanupProtocol` while the protocol file now defines `ISceneInspectionProtocol`/`ISceneCleanupProtocol`. This breaks the entire `modules.shared.src` import chain, so the full project test suite (and any test importing shared) cannot run. Decision: leave untouched (out of scope + actively edited by a sibling). If the sibling's refactor stalls, the fix is to (a) create `taxonomy_scene_vo.py` re-exporting the VOs from `taxonomy_scene_command_vo.py`, and (b) align protocol class names between aggregate and protocol file.
