# ARWAKY LOOP QUESTIONS

FRD ambiguities / open questions surfaced by the loop:

- (none yet)
- RESOLVED: cli `capabilities_cli_lifecycle.py` (and the entire cli agent/capability layer) was removed in cycle 25. Full-repo grep proved `CliContainer` was NEVER instantiated (the original DEFERRED note assumed it was in the bootstrap chain — false). The real CLI (`surface_cli_command.py`) routes through `core_agent_orchestrator` from the DI container. No bootstrap breakage; 347 tests pass.
- RESOLVED (cycle 28): AES503 orphans in job (×5) and mcp (×3) capabilities were VERIFIED DEAD via full-repo grep — job `JobOrchestrator` is self-contained (implements IJobAggregate, covers FR-JOB-001..005) and delegates to none of the 5 capabilities; mcp capabilities have zero consumers (2 with zero references anywhere). All 8 capabilities + orphaned protocols/taxonomy/utility (20 files) + 3 barrels deleted. Real functionality lives in JobOrchestrator / MCP surfaces, so FR coverage is preserved. job + mcp modules now COMPLIANT.
- OPEN (cycle 37, UPDATED cycle 39): Bulk remediation of the remaining authoritative lint-arwaky subset needs a user decision. **CRITICAL CAVEAT (cycle 39):** the AES501/AES504 "orphan taxonomy/utility" flags are UNRELIABLE — 8 of 9 flagged files are FALSE POSITIVES because lint-arwaky's orphan detector misses cross-module imports (e.g., `taxonomy_gateway_event.py` is consumed by 6 files; `taxonomy_job_error.py` by the job orchestrator). Only `utility_config_loader.py` is a genuine orphan, and it remains DEFERRED (cycle 38 excluded it for concurrent sibling local mods; stable + unused). **Do NOT drive the bulk orphan pass from lint-arwaky flags alone — every candidate needs a full-repo grep** (as cycles 28/37 did). The other non-deferred actionable codes: AES101/AES102 (naming renames), AES202 (mandatory taxonomy import → dummy import trips AES204/AES304), AES203/AES204 (dead imports), AES305 (duplication). Fixing AES202 and AES101/102 requires either dummy imports or cascading renames (high risk, collides with concurrent sibling agents). Recommend the user confirm the acceptable remediation strategy before a bulk pass.
- RESOLVED (cycle 40): Dangling `blender-arwaky` console-script entry. The root `pyproject.toml` `[project.scripts]` entry was already repointed by a concurrent edit to `blender-arwaky = "modules.cli.src.surface_cli_main:main"` (now line 48), which resolves to the existing `main() -> int:` in `modules/cli/src/surface_cli_main.py` (FR-CLI-001/002). The stale cycle-39 observation (`modules.cli.cli_main:main`) is no longer true — the CLI packaging refactor landed and corrected the entry. No change required from this agent; closing the question.
