[project]
name = "blender-arwaky-mcp"
version = "1.7.0"
description = "BlenderArwaky MCP surface-layer module"
requires-python = ">=3.10"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["."]
